import { SearchStartupPipe } from './search-startup.pipe';

describe('SearchStartupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStartupPipe();
    expect(pipe).toBeTruthy();
  });
});
